include: package:aelysia_flutter_lint/aelysia_flutter.yaml

analyzer:
  plugins:
    - riverpod_lint
  exclude:
    - "**/*.g.dart"
    - "**/*.freezed.dart"

plugins:
  riverpod_lint: ^3.0.3

linter:
  rules:
